# Used only for `npm run dev` outside Docker. When running via
# `docker compose up`, these are injected by docker-compose.yml instead.
VITE_API_BASE_URL=http://localhost:8000/api
VITE_WS_BASE_URL=ws://localhost:8000/ws
