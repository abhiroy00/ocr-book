# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
websockets==13.1
pydantic==2.10.3
pydantic-settings==2.6.1

# --- DB ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# --- Background processing ---
celery==5.4.0
redis==5.2.1

# --- Vision / PDF ---
# PyMuPDF alone covers rendering, vector/text PDF generation, and rasterizing
# for quality comparison, so ReportLab isn't pulled in as a second, redundant
# PDF-writing library.
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==11.0.0
PyMuPDF==1.25.1
scikit-image==0.24.0

# --- OCR ---
paddleocr==2.9.1
paddlepaddle==2.6.2
pytesseract==0.3.13

# --- DOCX ---
python-docx==1.1.2

# --- HTTP clients (NVIDIA / Ollama) ---
httpx==0.28.1

# --- Storage (S3) ---
boto3==1.35.76

# --- Utils ---
python-slugify==8.0.4
structlog==24.4.0
tenacity==9.0.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0
httpx[cli]==0.28.1
